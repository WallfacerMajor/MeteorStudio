# MeteorStudio 协作要求

本文件记录用户在长期迭代中已经明确确认的要求。后续代理开始工作前应先阅读本文件，再检查当前代码、项目文档、测试和 `git status`。不要因为新上下文丢失这些约束。

## 工作与发布规则

- 默认只在本地修改、测试和构建。只有用户明确说“推送”后才能推送 GitHub；不要把“搞完”理解成授权推送。
- 用户要求推送时，优先使用其指定的 GitHub 插件/集成，不要擅自改用本地凭据或重新走繁琐的设备登录。
- 工作区可能已有大量用户修改和未跟踪文件。保留它们，不要重置、清理、覆盖或删除与当前任务无关的内容。
- 每次声称修复前必须运行与问题对应的真实交互回归测试；重要修改还要运行完整单元测试和打包版测试。不要仅凭代码阅读或立即帧测试宣称完成。
- Windows 本地构建产物位于 `dist/MeteorStudio/MeteorStudio.exe`。项目必须继续支持 macOS 构建，不能引入仅 Windows 可用的核心逻辑。

## 产品定位与素材安全

- MeteorStudio 是跨平台的流星筛选、星空对齐、流星蒙版、合成及延时视频处理工具。
- 输入素材必须只读。不得移动、改名、覆盖或修改原始 RAW/TIFF/JPG、PTGui 工程和底图。
- 输出目录必须与输入/流星素材目录分开；程序创建独立的带时间戳输出目录，并提供“打开文件夹”。不要设置没有必要的路径限制。
- 文件配对按不含后缀的同名主文件名完成，例如 TIFF/RAW 与同名 JPG。不得要求用户逐张双击加载。
- 项目及自动保存必须保留蒙版、候选、锁定、每颗流星参数、变换、来源模式、路径、阈值和用户判断。

## 三个主要工作流

### 流星批量筛选

- 支持 JPG/PNG/TIFF，以及 Sony ARW、Nikon NEF/NRW、Canon CR2/CR3/CRW；RAW 只在内存中解码。
- 连续照片分析默认结合前后最多 3 张（界面高清缓存保留最近 5 张），用时序信息排除固定结构、星点配准残差、飞机和卫星。
- 必须先动态判断每张照片的星空区域，再在星空区域内找流星；不能用固定高度裁掉地景。
- 候选生成与合成工作区复用同一套特征和本地 AI 模型。
- 照片级“保留/排除”只决定导出，不能直接成为 AI 的候选标签。只有用户明确确认“这条是流星/不是流星”或手动补标的具体轨迹才能训练 AI。
- 同一条流星的亮核、弱尾迹和中间断段必须合并为一个候选。合并要考虑方向共线、横向距离、轴向间隙和连续性，不能简单合并所有邻近直线。
- DSC08033、DSC08083 是已出现过“一条流星被拆成多条”的回归样本。DSC08083 的真实流星断成约 25–39 px 的三段、间隙约 27–32 px；旁边还有一条 41 px 的弱竖向残差。真实轨迹应合并为一条，弱残差默认不得自动成为第二颗，但仍可作为低分候选供用户选择。
- 若一张照片的所有候选都被明确排除且没有手动真实流星，照片应自动排除。
- 列表用颜色区分状态；预览中的候选导轨不得遮住流星本身；支持查看无标记原图。

### Siril 星点 + PTGui 对齐

- Siril 用于找星点，PTGui 按用户提供文章中的已验证思路完成对齐；不要擅自增加不必要的 PTGui 操作。
- 焦距从 EXIF 动态读取，不能写死；星空蒙版按每张图动态判断。
- 最终保持“一张图一个独立 PTGui 工程”。可抽取少量图片共享标定/镜头先验，但不能把所有图强塞进一个工程。
- 尽量对齐；对不好的图片也要输出并标记为待处理，允许用户切换“自动对齐素材/原始素材”和一键恢复。
- 不得绑定或反复寻找某个固定文件（例如 DSC04890）；底图和参考图都由当前项目决定。
- 一张图可能有多颗流星：每颗流星可独立选择自动对齐素材或原始素材。不能因为其中一颗需要原始状态，就让同图其他流星全部失去对齐。

### 流星合成

- 开始时明确选择输出模式：
  - 拼合到同一张底图：只能选择一张共享底图，最终预览/导出必须包含所有启用流星。
  - 分别输出：允许多张底图或文件夹，同名逐张配对。
- 界面术语应准确：星空对齐阶段称“对齐图”；合成模块才使用底图概念。主入口称“流星合成功能”，不要使用含糊的“素材和输出”。
- `干净底图` 视图绝不能显示红色蒙版、候选或来源标记；`总融合预览` 必须显示所有流星，而非只显示当前图。
- 导出必须同时支持最终无标记图和标注流星来源的图；TIFF 必须真正包含所有修改，不能只修改 JPG 预览。

## 蒙版与直接编辑交互

- 手绘行为必须像 Photoshop 画笔：每一笔累加为并集，下一笔绝不能删除或替换上一笔；靠近、重叠、交叉绘制同样如此。
- 普通拖动画笔；单击锚点后按住 Shift 再单击绘制直线连接。Shift 连接之后的下一次普通绘制仍须保留已有蒙版。
- 画笔和橡皮擦宽度独立。光标应显示实际圆形尺寸，而不是固定十字架；橡皮擦应有明确图标/轮廓。
- 按住 Alt/Option 临时切换画笔与橡皮擦，松开恢复；必须防止系统吞掉松开事件后工具状态卡死。
- 红色蒙版默认显示；按住 H 临时隐藏，松开恢复，而不是永久开关。
- 右键蒙版可删除整条、锁定或进入变换；锁定项不受阈值变化、重新检测和清除未锁定蒙版影响。
- 候选应一直可见。鼠标移近候选时出现可点击的“＋选中”，点击必须立即生成并锁定对应蒙版，不能出现无法点击的悬浮窗。
- 自动检测只负责提出带分数候选；用户能调“候选更多/候选更少”、锁定、删除、补画和橡皮擦修正。
- 蒙版宽度、延伸和羽化应根据每颗流星亮核、光晕和弱尾迹自适应，不能使用一刀切的粗蒙版，也不能漏掉弱尾迹。

## 每颗流星的融合与变换

- 每颗流星都有独立设置：亮度、背景净化、饱和度、曝光匹配、混合模式、羽化、自动优化和来源模式；不能只按整张图片设置。
- 自动融合优化需分别估算底图/原图局部亮度、色偏、噪声和自适应黑场，校正低频背景差异，保护连续亮核与弱尾迹，只压制方向/连续性不符的暗部色块。
- 默认融合不能明显压暗流星。局部曝光匹配降低背景时，应保护或补偿流星亮部；可用流星曲线压暗残余背景并提升亮部，但不能用固定统一曲线替代逐颗分析。
- 羽化必须参与真实导出，解决流星周边色块、暗部方块和色偏接缝；预览与导出应尽量一致。
- 在最终融合/来源标注画布中，用户可以直接移动、旋转、拉伸和缩放每颗流星。移动的是流星像素和蒙版整体，绝不是只移动蒙版。
- 原始位置/形态和变换后状态均需在来源标注中清楚区分，并支持一键恢复。使用原始素材或人工变换的流星需使用特殊标注。
- 投影后落在画面外的流星不能静默丢失；保留为待处理对象，允许丢弃、切换原始状态或手动移动/旋转/拉伸到画面内。

## 预览、缩放与界面

- 主画布从一开始就使用原始像素数据，不设置额外“生成并打开精准预览”门槛。允许适合窗口、1:1、滚轮缩放和拖动；窗口缩放时正确重排。
- 快速帧替换为完整分辨率帧时，必须保持相同可见画幅，不能因像素尺寸改变而突然放大。
- 点击任何面板、标签页或底部空白区域都不得改变图片缩放、中心、显示边界或画布尺寸。只有明确缩放操作或画布真实尺寸变化才能改变视口。
- 进入合成工作区不能黑屏，默认应显示可用的总融合预览。上下切换图片应自动加载并使用缓存，无需双击。
- 顶层工作区只能有一个；点击菜单不能再打开第二个并存工作区。
- 所有耗时操作给出明确状态；错误信息必须可选择、复制，并能在应用内“运行日志”查看，不能只说“已打印到终端”。

## 性能红线

- 多线程、内存缓存、LRU、预取和任务合并应贯穿扫描、RAW 解码、候选分析、预览与合成；避免 OpenCV 嵌套线程过量。
- 修改一颗流星（参数、自动优化、移动、旋转、拉伸、恢复、来源切换、删除）只能重算其旧/新覆盖区域及确实相交的局部层。
- 单颗修改绝不能回退为：总融合全部重建 → 原始像素精准预览再全部重建。缓存淘汰时只加载受影响的图层并重算 ROI。
- 局部结果提交后必须取消遗留的总融合请求、原始像素精准请求和延迟整图验证任务，避免操作结束 320–1200 ms 后又开始整图计算。
- 调整单颗滑块应实时更新对应像素 ROI；不要重建画布图片对象，不要完整复制/重绘 8K 图，不要在停止拖动后重复计算同一结果。
- 添加/删除一条蒙版、撤销/重做和删除一颗流星同样优先局部更新。只有真正的全局设置变化或“自动优化全部流星”才允许整图任务。
- 正式导出仍必须从原始素材按完整 16 位链路重新计算；编辑预览的局部缓存优化不能降低导出质量。

## 回归测试要求

- UI 问题使用真实 Tk 坐标和事件测试，不要只调用内部函数。
- 涉及延迟任务时，测试必须等待超过实际延迟窗口再断言。单颗编辑至少覆盖自动精准预览约 320 ms 和整图验证约 1200 ms；立即检查后撤掉监控属于无效测试。
- “空白点击不缩放”需同时检查可见显示边界、归一化中心、画布分配和延迟后的状态，不能只比较数值缩放倍率。
- 单颗编辑测试必须监控全局失效、总融合 worker、精准预览 worker及遗留 `after` 任务，断言全部未启动。
- 缓存淘汰回归需验证仍能只读取相交图层并更新局部。当前真实项目是 7952×5304，已有验证示例只更新约 169×167 的区域。
- 每次本地交付至少执行：`python -m unittest discover -p "test_*.py"`、`python editable_composite_smoke.py`、重新打包，以及对打包 EXE 运行 `METEOR_EDITABLE_SMOKE_REPORT` 交互测试。

## 视频功能

- 支持单视频动态流星效果：无流星区域按用户设置速度播放，流星出现部分使用可调速度曲线/延长效果，避免“背景停住但流星仍迅速消失”。
- 保持原始画幅、分辨率和高码率质量；不能擅自降低码率导致文件从约 300 MB 变成约 100 MB。
- 导出前后检查整帧色块/编码伪影，不只检查用户最初指出的右下角。输出尺寸或编码需要补齐时使用安全 padding，不能裁掉画面。
